# 排除 Visual Studio 自动生成的缓存和设置
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# 排除编译生成的二进制文件 (这就是导致你刚才报错的主因)
bin/
obj/

# 排除 SQLite 数据库文件 (因为版本间结构不同，不建议进 Git)
*.db
*.db-shm
*.db-wal

# 排除发布文件夹
publish/